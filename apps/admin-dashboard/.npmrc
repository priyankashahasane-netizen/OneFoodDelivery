# Strict peer dependencies to prevent dependency conflicts
legacy-peer-deps=false
strict-peer-deps=true

# Use exact versions from package-lock.json to prevent unexpected updates
package-lock=true
save-exact=false







